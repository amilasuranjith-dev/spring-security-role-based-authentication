spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/spring_security_db?createDatabaseIfNotExist=true}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:1234}

  flyway:
    baseline-on-migrate: true
    locations: classpath:db.migration

app:
  jwt:
    secret: ${JWT_SECRET:6fec7c120c1a32c708a30f3faead7aced56e8c8fd0848b63861b2c3101e19a2d}
    expiration:
      ms: ${JWT_EXPIRATION_MS:86400000}
